# CreateEnterpriseConnectionProvider

The identity provider (e.g. saml_custom, oidc_custom)

## Example Usage

```ruby
require "dctest-sdk-ruby"

value = CreateEnterpriseConnectionProvider::SAML_CUSTOM
```


## Values

| Name             | Value            |
| ---------------- | ---------------- |
| `SAML_CUSTOM`    | saml_custom      |
| `SAML_OKTA`      | saml_okta        |
| `SAML_GOOGLE`    | saml_google      |
| `SAML_MICROSOFT` | saml_microsoft   |
| `OIDC_CUSTOM`    | oidc_custom      |